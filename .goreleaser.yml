version: 2

builds:
  - id: kubespectre
    main: ./cmd/kubespectre/
    binary: kubespectre
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

# WO-27: brews MUST stay after archives + checksum so the Homebrew formula only
# ever references assets GoReleaser has already built and checksummed in this run
# (asset-before-formula). internal/release/workflow_contract_test.go pins this.
brews:
  - repository:
      owner: ppiankov
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/ppiankov/kubespectre"
    description: "Kubernetes security posture auditor"
    license: MIT
    install: |
      bin.install "kubespectre"
    test: |
      system "#{bin}/kubespectre", "version"
